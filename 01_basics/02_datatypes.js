"use strict " //now all code will be treated as newer version code.

// there is a different syntax for using alert in node.


// data types

let var1="himani"; //string 
let var2=14; //number which has a range of 2 to the power 53 there is bigint as well
let isAvail=false; //boolean

//null=> we have null as well which is a standalone value in js 
//null doesn't mean the value is undefined it means the variable is empty
// undefined => if value is not assigned
//symbol => normally used for specifying uniqueness.
// object

console.log(typeof null);
