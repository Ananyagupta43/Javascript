//make sure to compare the same type of datatypes.

//supoose we compare null and 0 then we get unexpected results like 
//try to avoid these type of conversions

console.log(null>0);
console.log(null==0);
console.log(null>=0);

// strict check => === not only checks the value but the type as well.

